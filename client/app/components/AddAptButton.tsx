'use client';
import React from 'react'

const AddAptButton = () => {
  return (
    <div>
        <button onClick={() => console.log('Click')}> Add Apartment </button>
    </div>
  )
}

export default AddAptButton
